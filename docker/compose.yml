name: bot-telegram

networks:
  default:
    name: bot-telegram
    driver: bridge

services:
  webapp:
    container_name: webapp
    build:
      context: ../
      dockerfile: ./docker/php/Dockerfile
    volumes:
      - ../:/var/www/html
      - ./php/php.ini:/usr/local/etc/php/php.ini
    ports:
      - ${WEBAPP_PORT:-80}:80
    networks:
      - default
    depends_on:
      - mysql
  
  mysql:
    container_name: mysql
    image: mysql:8.4
    networks:
      - default
    environment:
      MYSQL_ROOT_PASSWORD: ${WEBAPP_PORT:-root}
      MYSQL_DATABASE: testdb
  
  phpmyadmin:
    container_name: phpmyadmin
    image: phpmyadmin:5.2
    ports:
      - ${PMA_PORT:-8080}:80
    networks:
      - default
    depends_on:
      - mysql
    environment:
      PMA_HOST: mysql
